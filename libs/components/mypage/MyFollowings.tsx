import { Stack, Typography } from "@mui/material";

const MyFollowings = () => {
  return (
    <Stack className="followings-content">
      <Typography className="content-title">Followings</Typography>
    </Stack>
  );
};

export default MyFollowings;
